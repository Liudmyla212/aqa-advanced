const PI = Math.PI;
const radiusCylinder = 3.4;
const heightCylinder = 4.5;
// The formula for calculating the volume of a cylinder 
const volumeCylinder = PI*Math.pow(radiusCylinder, 2)*heightCylinder;
// Result rounded to 2 decimal places
console.log(volumeCylinder.toFixed(2))